{
  "name": "sintija-dance-web",
  "version": "1.0.0",
  "private": true,
  "description": "sintijadance.lv — Eleventy static site (structured-source architecture, mirrors resonatekit-web)",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
